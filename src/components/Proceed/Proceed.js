import React from 'react';
import image from '../../images/giphy.gif'
const Proceed = () => {
    return (
        <div>
            <img src={image} alt=""/>
        </div>
    );
};

export default Proceed;